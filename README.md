# jumbo youtube assignment
 A backend API using NestJS, TypeORM, and PostgreSQL that fetches the latest videos from YouTube via its API, stores them in a PostgreSQL database, and allows user authentication.
